/*
import React from 'react';
import { expect } from 'chai';
import { render } from 'enzyme';
import PromotionalModal from './../promotionalModal';
import Json from '../banner.component.json';

describe('<PromotionalModal />', () => {
  it('should render <PromotionalModal /> component.', () => {
    const { cms } = Json.context.data;
    const wrapper = render(<PromotionalModal {...cms} />);
    expect(wrapper.find('div')).to.have.length(1);
  });
});
*/
