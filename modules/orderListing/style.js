import { css } from 'react-emotion';

const blueColor = css`
    color: #0055a6;
`;

const dropDown = css`
  li {
    min-height: auto;
    font-size: 0.9rem;
   }
`;

export { blueColor, dropDown };
