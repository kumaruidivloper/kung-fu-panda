import { css } from 'react-emotion';

export const contentWrapper = css`
  display: inline-block;
  outline: none;
  transform: translateY(-25%);
`;

export const popoverStatelessWrapper = css`
  margin-top: 2px;
  max-height: 18px;
  overflow: visible;
`;
